# Thư mục Load Test MarketPlace

Thư mục này dùng để lưu trữ các kịch bản K6 (ví dụ: `gd1-products.js`, `gd2-cart-order.js`) và các file báo cáo Markdown (MD) liên quan đến việc Load Test hệ thống MarketPlace.

Vui lòng push các file kết quả vào đây.
