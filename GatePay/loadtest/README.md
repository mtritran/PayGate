# Thư mục Load Test PayGate

Thư mục này dùng để lưu trữ các kịch bản K6 (ví dụ: `gd3-idempotency.js`, `gd4-webhook-load.js`) và các file báo cáo Markdown (MD) liên quan đến việc Load Test hệ thống PayGate.

Vui lòng push các file kết quả vào đây.
